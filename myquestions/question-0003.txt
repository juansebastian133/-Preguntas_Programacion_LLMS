3. Ingeniería de Características Temporales y Regresión RegularizadaEl Problema:Recibes un DataFrame con ventas diarias. Para predecir las ventas del día siguiente, necesitas crear características de "lag" (el valor del día anterior) y luego entrenar un modelo que evite el sobreajuste mediante penalización $L1$.Tu Misión:Escribe una función que cree una columna ventas_ayer usando shift() de pandas, elimine los nulos resultantes y entrene un modelo de Lasso.Nombre de la función: entrenar_pronostico_lassoArgumentos: df (pd.DataFrame), target_col (str), alpha_lasso (float)Tipo de objetivo: tuple (el modelo entrenado y el coeficiente $R^2$ en el mismo set)