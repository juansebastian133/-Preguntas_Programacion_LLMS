1. Optimización de Hiperparámetros con Validación CruzadaEl Problema:Trabajas con un dataset de clasificación de imágenes médicas (representadas por vectores numéricos) y quieres encontrar el mejor valor del parámetro de regularización ($C$) para un modelo de Soporte de Vectores (SVC). No basta con una división simple; debes asegurar la robustez mediante una búsqueda en rejilla.Tu Misión:Escribe una función que reciba los datos y una lista de valores para $C$, realice una búsqueda con validación cruzada y devuelva el mejor modelo junto con su puntuación.Nombre de la función: optimizar_svm_gridArgumentos: X (np.ndarray), y (np.ndarray), lista_c (list), cv_folds (int)Tipo de objetivo: dict (con las claves 'mejor_c', 'mejor_score' y 'modelo_final')